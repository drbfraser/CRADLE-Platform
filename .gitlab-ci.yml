stages:
  - build
  - test
  - format
  - deploy

# Frontend Build Job
build frontend:
  stage: build
  image: node:10
  tags:
    - docker
  before_script:
    - cd client
    - npm install
  script:
    - CI=false yarn build
