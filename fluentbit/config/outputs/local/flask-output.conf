[OUTPUT]
    Name file
    Path ${OUTPUT_DIR}
    Match flask.access

[OUTPUT]
    Name file
    Path ${OUTPUT_DIR}
    Match flask.error

[OUTPUT]
    Name file
    Path ${OUTPUT_DIR}
    Match flask.application
