import { Box, styled } from '@mui/material';

export const Header = styled(Box)(() => ({
  display: 'flex',
  flexDirection: 'row',
  marginY: '12px',
  alignItems: 'center',
  placeContent: 'space-between',
}));
