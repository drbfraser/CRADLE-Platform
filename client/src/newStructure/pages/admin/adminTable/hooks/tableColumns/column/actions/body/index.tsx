import React from 'react';
// import { useStyles } from './styles';

interface IProps {
  className: string;
  id: string;
}

export const ActionsBody: React.FC<IProps> = ({ className, id }) => {
  // const classes = useStyles();

  return null;
};
