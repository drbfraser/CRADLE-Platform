import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles({
  container: {
    padding: `35px 25px`,
    borderRadius: 15,
  },
  input: {
    appearance: `none`,
    margin: 0,
  },
});
